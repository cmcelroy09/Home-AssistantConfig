---
# The Home Assistant Cloud allows you to quickly integrate your local
# Home Assistant with various cloud services like Amazon Alexa and Google Home.
#
# https://www.home-assistant.io/components/cloud/
#
cloud:
  # Customize the Amazon Alexa integration with the Naby Casa Cloud
  # alexa:

    # Filters for entities to include/exclude from Amazon Alexa.
    # These are loaded from a separate directory.
    # filter: !include_dir_merge_named ../assistants/alexa/filters

    # Entity specific configuration for Amazon Alexa.
    # These are loaded from a separate directory.
    # entity_config: !include_dir_merge_named ../assistants/alexa/entities

  # Customize the Google Assistant integration with the Naby Casa Cloud
  google_actions:

    # Filters for entities to include/exclude from Google Assistant.
    # These are loaded from a separate directory.
    filter: !include_dir_merge_named ../assistants/google/filters

    # Entity specific configuration for Google Assistant.
    # These are loaded from a separate directory.
    entity_config: !include_dir_merge_named ../assistants/google/entities